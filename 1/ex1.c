#include <stdio.h>

/* This is a comment */
int main(int argc, char *argv[]) {
  int distance = 100;

  // this is also a comment
  printf("You are %d miles away.\nI just started.\n", distance);
  printf("Let's just try this one %d\n", distance*distance);

  return 0;
}
